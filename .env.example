# =============================================================================
# CHAI BACKEND - ENVIRONMENT VARIABLES
# =============================================================================
# This is a standalone backend application running on port 3001
# Copy this file to .env and configure your values
# SECURITY: Never commit .env to version control
# Generate secrets: openssl rand -base64 32

# =============================================================================
# SERVER CONFIGURATION
# =============================================================================
PORT=3001
NODE_ENV=development

# Frontend URL for CORS (CRITICAL: Must match your frontend URL)
# Development: http://localhost:5174
# Production: https://yourdomain.com
FRONTEND_URL=http://localhost:5174

# =============================================================================
# DATABASE
# =============================================================================
DATABASE_URL=postgresql://username:password@localhost:5432/chaiapp_db

# =============================================================================
# SECURITY & AUTHENTICATION
# =============================================================================

# JWT Secret - CRITICAL: Change in production
# Generate: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-min-32-chars-CHANGE-THIS

# Session Secret - CRITICAL: Change in production
# Generate: openssl rand -base64 32
SESSION_SECRET=your-session-secret-key-CHANGE-THIS

# Session Configuration
SESSION_MAX_AGE=604800000

# =============================================================================
# GOOGLE OAUTH
# =============================================================================
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret

# =============================================================================
# PRODUCTION SECURITY SETTINGS
# =============================================================================
# Uncomment and configure for production:

# HTTPS Enforcement (set to 'true' in production)
# FORCE_HTTPS=true

# Allowed Admin IPs (comma-separated, optional)
# ADMIN_IP_ALLOWLIST=203.0.113.1,198.51.100.0/24

# Rate Limiting (requests per window)
# RATE_LIMIT_ADMIN=20
# RATE_LIMIT_AUTH=10
# RATE_LIMIT_GENERAL=100

# =============================================================================
# EMAIL CONFIGURATION (for password reset, notifications)
# =============================================================================
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-specific-password
# SMTP_FROM=noreply@yourdomain.com
