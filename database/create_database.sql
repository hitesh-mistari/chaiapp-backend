-- Database Creation Script
-- Run this script to create the database before running schema.sql

-- Drop database if it exists (Optional - Use with caution)
-- DROP DATABASE IF EXISTS chai_db;

CREATE DATABASE chai_db;
